{
    "private": true,
    "name": "devconnector-monorepo",
    "workspaces": ["packages/*"],
    "scripts": {
        "start:ci": "yarn workspace server start",
        "client": "yarn workspace client start",
        "server": "yarn workspace server start",
        "start": "concurrently --kill-others-on-fail \"yarn server\"  \"yarn client\"",
        "build": "NPM_CONFIG_PRODUCTION=false  yarn workspace client build",
        "test": "yarn workspace tests test"
    }
}